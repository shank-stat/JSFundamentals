// All primitive data Types are Immutable - Cannot change original state

let day ="Sunday"
console.log(day)
console.log(day.toUpperCase()); // Immutable operation, original string remains unchanged

// Original states for Objects like Array or Objects can be chaged.

//Except for undefined and null, every other data typehave wrapper object types (starts with uppercase)
let num = Number(5);
console.log(num);

